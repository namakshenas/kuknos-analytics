/**
 * Tokens selectable in the buys / refunds dashboards.
 * Codes must match the `code` column values in pending_txes / pending_refunds.
 */
export const TOKENS = ['PMN', 'IRT', 'DAYADIAMOND'];

export const DEFAULT_TOKEN = 'PMN';
